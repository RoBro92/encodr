[pytest]
testpaths = tests
markers =
    unit: fast, isolated tests with mocked boundaries
    integration: tests that exercise real service and persistence wiring
    e2e: realistic vertical-slice tests across the local stack
    smoke: lightweight runtime and boot validation checks
    security: auth and security-regression coverage
