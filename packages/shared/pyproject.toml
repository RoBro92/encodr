[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "encodr-shared"
version = "0.1.0"
description = "Shared types for encodr"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["encodr_shared"]

