# Core Package

Shared domain logic for media probing, policy loading, planning, renaming, verification, and config handling.

This package is intended to hold deterministic business rules that can be reused by the API and worker applications.

