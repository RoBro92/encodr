# Tests

Test layers planned for this repository:

- `unit/` for deterministic policy, planning, naming, and verification tests
- `integration/` for database, queue, API, and ffprobe integration tests
- `e2e/` for UI and workflow-level coverage
- `fixtures/` for anonymised media metadata fixtures and sample plan inputs

No test suite has been implemented yet.

