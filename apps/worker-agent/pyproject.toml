[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "encodr-worker-agent"
version = "0.1.0"
description = "Remote worker agent placeholder for encodr"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["app"]

