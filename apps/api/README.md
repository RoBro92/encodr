# API App

FastAPI service for authentication, queue orchestration, job inspection, dashboard data, configuration views, and future control endpoints.

Current scope:

- boot a minimal FastAPI application
- expose a placeholder health endpoint
- reserve module boundaries for API routes, schemas, services, and domain-facing models

Business logic is expected to live primarily in shared packages rather than directly in this application layer.

